# =============================================================================
# Variables de Entorno para Keycloak HA
# PostgreSQL + Patroni + pgpool + etcd
# Copiar este archivo a .env y ajustar los valores
# =============================================================================

# Credenciales de Admin de Keycloak
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_this_password

# PostgreSQL - Usuario de aplicación (Keycloak)
POSTGRES_PASSWORD=keycloak_secret

# PostgreSQL - Usuario administrador (para Patroni y pgpool)
POSTGRES_ADMIN_PASSWORD=postgres_admin

# PostgreSQL - Usuario de replicación (para Patroni)
REPLICATION_PASSWORD=replicator_secret

# Patroni - Contraseña del administrador
PATRONI_ADMIN_PASSWORD=admin_secret

# pgpool - Contraseña del administrador
PGPOOL_ADMIN_PASSWORD=admin_secret

# Puertos (para pruebas locales)
# NODO 1
KC_PORT=8443
JGROUPS_PORT=7800

# NODO 2
# KC_PORT=8444
# JGROUPS_PORT=7801

# Logging
KC_LOG_LEVEL=info

# Certificados
CERT_DOMAIN=localhost
CERT_DAYS=3650
